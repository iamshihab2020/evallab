/**
 * Shadcn Button shim — re-exports the lab/button so every existing import
 * (`@/components/ui/button`) automatically picks up the redesign.
 */
export { Button, type LabButtonProps as ButtonProps } from "@/components/lab/button";
